apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: istio-demo-v1
spec:
  selector:
      app: istio-demo-v1
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - version1.hicham.com
